# ============================================================
# NEXORA — Machine Translation with Seq2Seq + Attention
# Python 3.10+ (works on Streamlit Cloud's Python 3.14 too)
# Install: pip install -r requirements.txt
# ============================================================

# Dashboard (pure-Python — let pip resolve)
streamlit>=1.41

# Core data + numerical (latest has cp314 wheels)
numpy==2.2.6
pandas==2.3.3

# Plotting
matplotlib==3.10.9
wordcloud==1.9.6

# Classical ML + NLP feature engineering
scikit-learn==1.7.2
nltk>=3.9

# Production translation engine (M2M100 + tokenizer)
# (facebook/m2m100_418M is downloaded lazily only when you press Translate)
transformers>=4.46
sentencepiece==0.2.2
sacremoses>=0.1.1

# Experimental Seq2Seq + Attention deep learning (CPU build saves ~2 GB)
torch==2.12.0

# Dataset streaming (only the Samanantar metadata is streamed — tiny)
datasets>=3.2

# Memory guard for Live Translation engine (psutil reads free RAM)
psutil==7.2.2